---
date: 2026-05-23
type: evening
---

# Вечерняя рефлексия — 2026-05-23

## ✅ Успехи дня

- Вечерняя сводка выполнена штатно, в штатном режиме.
- Gmail fallback на himalaya работает стабильно, OAuth по-прежнему не восстановлен.
- Email digest timestamps обновлены корректно.

## 🔴 Незавершённое

- **Gmail OAuth-токен** — по-прежнему истёк (refresh token мёртв с апреля). Fallback на himalaya работает, но browser-based re-auth недоступна в cron. Нужна ручная сессия с `hermes agent google-workspace setup` при следующем интерактивном запуске.
- Timestamp-файл `email_digest_last_run.json` живёт по пути `/home/lighthouse/.hermes/` (верный путь), но утренние запуски 22 и 23 мая не обновили `last_morning_timestamp` — последнее значение всё ещё `2026-05-21T07:03:51+00:00`. Пропущено 2 утренних запуска.

## 📌 GBrain:

- Критичный путь для email-digest: файл timestamps всегда `/home/lighthouse/.hermes/email_digest_last_run.json` (не `/root/.hermes/`). Утренний запуск должен писать `last_morning_timestamp = now` перед выходом; вечерний — `last_evening_timestamp = now`.
- При пропуске утреннего запуска вечерний будет показывать delta с последнего реального утреннего ts, что искажает digest. Если пропущено ≥2 утренних — ручная корректировка `last_morning_timestamp` перед evening run.
- himalaya query для датированного поиска: `himalaya envelope list --folder INBOX --output json "after YYYY-MM-DD" "order by date desc"` — flags до query string, operators space-separated.